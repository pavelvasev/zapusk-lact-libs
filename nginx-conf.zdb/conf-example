# auto generated by chroot-tool
upstream sisq {
  server 127.0.0.1:4567;
}

server {
   server_name  nano.horse-11.ru nano.lact.in;
   listen       80;
   root         CDIR/apps/selectimagesq;

   # самое простое дубовое решение.. см todo
   expires -1;

   try_files    $uri @services;

   # макс размер загружаемых данных
   client_max_body_size 10M;

   location @services {
       proxy_set_header  X-Real-IP        $remote_addr;
       proxy_set_header  X-Forwarded-For  $proxy_add_x_forwarded_for;
       proxy_set_header  Host             $http_host;
       proxy_redirect    off;
       proxy_pass        http://sisq;
   }
   error_page   500 502 503 504  /50x.html;
}