Производит встраивание 1го компонента в целевой employ-объект.

Вход: 
  file - полный путь к файлу с ini-описанием, которое надо встроить
  target - полный путь к zdb-каталогу, поддерживающему команду employ
  
Команды:
  apply - вызвать employ у target с указанием file на входе (в параметре запроса copath)
  destroy - вызвать unemploy
  
Примечание
destroy вызывает unemploy с указанием сохраненной копии файла (которая была вызвана на apply)
с тем чтобы при изменении данных по пути file сохранились исходные данные
(на самом деле похоже важен только global name)